# 05 — Console Class `YCL_PAYMENT` (snapshot)

_ยังไม่มี — จะเติมหลังได้ test data จาก [04-data-export-sql.md](04-data-export-sql.md)
และผู้ใช้ confirm ว่าพร้อมรับ code_

source of truth คือ tenant · ไฟล์นี้เป็น snapshot ไว้อ่านเท่านั้น
